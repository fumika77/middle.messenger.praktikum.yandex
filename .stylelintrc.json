{
  "extends": "stylelint-config-standard",
  "rules": {
    "color-hex-case": "lower",
    "selector-class-pattern": "^([a-zA-Z)(__[a-zA-Z0-9])+$"
},
  "ignoreFiles": [
    "dist/*",
    "node_modules/*"
  ]
}